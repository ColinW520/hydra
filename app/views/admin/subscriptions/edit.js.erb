// Render the form
$('.modal-body').html('<%= j render "admin/plans/form" %>');

// Show the dynamic dialog
$('#plan_dialog').modal("show");

// Set focus to the first element
$('#plan_dialog').on('shown.bs.modal', function () { })

$("#plans").smart_listing().fadeLoaded();
