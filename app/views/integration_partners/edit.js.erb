// Render the form
$('.modal-body').html('<%= j render "integration_partners/form" %>');

// Show the dynamic dialog
$('#integration_partner_dialog').modal("show");

// Set focus to the first element
$('#integration_partner_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#integration_partners").smart_listing().fadeLoaded();
