- provide(:title, "Edit #{@line.name} Line")
.row
  .col-sm-12
    %h4
      %i.fa.fa-hashtag
      Lines
.row
  .col-sm-12.col-md-6.col-centered
    .card.card-default
      .card-block
        = render 'form'
    .text-center
      = link_to line_path(@line), method: :delete, data: { confirm: "Are you sure? You will no longer be able to use this line for sending messages. History will be preserved." }, class: 'text-center text-danger' do
        Need to Remove this Line?
