// Render the form
$('.modal-body').html('<%= j render "messages/form" %>');

// Show the dynamic dialog
$('#message_dialog').modal("show");

// Set focus to the first element
$('#message_dialog').on('shown.bs.modal', function () {$('#InputBody').focus()})

$("#messages").smart_listing().fadeLoaded();
