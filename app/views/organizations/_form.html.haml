= simple_form_for [@organization], remote: @xhr, authenticity_token: true, html: { id: 'OrganizationsForm' } do |f|
  = f.input :created_by, as: :hidden, input_html: { value: current_user.id }
  .form-group
    = f.input :name, id: 'InputName'
  .form-group
    = f.input :email_domain, id: 'InputEmailDomain'
  .form-group
    = f.input :city, id: 'City'
  .form-group
    = f.input :state, id: 'State'
  .form-group
    = f.input :zip, id: 'Zip'
  .form-actions
    .text-center
      = f.button :submit, class: 'btn btn-md btn-success'
