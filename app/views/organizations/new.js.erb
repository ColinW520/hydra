// Render the new form
$('.modal-body').html('<%= j render("organizations/form") %>');

// Show the dynamic dialog
$('#organization_dialog').modal("show");

// Set focus to the first element
$('#organization_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})
