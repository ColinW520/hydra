// Render the form
$('.modal-body').html('<%= j render "subscriptions/form" %>');

// Show the dynamic dialog
$('#subscription_dialog').modal("show");

// Set focus to the first element
$('#subscription_dialog').on('shown.bs.modal', function () {$('#InputNickname').focus()})
